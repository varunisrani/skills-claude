# =============================================================================
# GitHub Repository Management App - Environment Variables (Simplified)
# =============================================================================
# This app uses localStorage instead of a database - no complex setup needed!
# Copy this file to .env.local and fill in your values (optional)
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# Node environment (development, production, test)
NODE_ENV=development

# Application URL (optional - mainly for OAuth callback if using OAuth)
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Application name (optional)
NEXT_PUBLIC_APP_NAME="GitHub Repo Manager"

# -----------------------------------------------------------------------------
# GitHub Authentication (OPTIONAL)
# -----------------------------------------------------------------------------
# OPTION 1: Personal Access Token (Recommended for simplicity)
# Just login via the app UI - no env variables needed!
# Get your token at: https://github.com/settings/tokens/new?scopes=repo,user,read:org

# OPTION 2: GitHub OAuth (Optional - for more professional setup)
# Only needed if you want to implement OAuth flow instead of token input
# Get these from: https://github.com/settings/developers

# GitHub OAuth Client ID (optional)
GITHUB_CLIENT_ID=

# GitHub OAuth Client Secret (optional)
GITHUB_CLIENT_SECRET=

# GitHub API Base URL (usually no need to change)
NEXT_PUBLIC_GITHUB_API_URL=https://api.github.com

# -----------------------------------------------------------------------------
# Feature Flags (Optional)
# -----------------------------------------------------------------------------

# Enable/disable features
NEXT_PUBLIC_FEATURE_DARK_MODE=true
NEXT_PUBLIC_FEATURE_CACHE=true
NEXT_PUBLIC_FEATURE_SEARCH=true
NEXT_PUBLIC_FEATURE_FAVORITES=true

# -----------------------------------------------------------------------------
# Cache Configuration (Optional)
# -----------------------------------------------------------------------------

# Cache TTL (Time To Live) in milliseconds
# Default: 5 minutes (300000 ms)
NEXT_PUBLIC_CACHE_TTL=300000

# Enable localStorage caching
NEXT_PUBLIC_ENABLE_CACHE=true

# Maximum cache size (number of items)
NEXT_PUBLIC_MAX_CACHE_ITEMS=100

# -----------------------------------------------------------------------------
# UI Configuration (Optional)
# -----------------------------------------------------------------------------

# Default theme (light, dark, system)
NEXT_PUBLIC_DEFAULT_THEME=system

# Items per page
NEXT_PUBLIC_ITEMS_PER_PAGE=30

# Default repository view (grid, list)
NEXT_PUBLIC_DEFAULT_REPO_VIEW=grid

# -----------------------------------------------------------------------------
# Analytics (Optional)
# -----------------------------------------------------------------------------

# Google Analytics Measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Vercel Analytics (automatically enabled on Vercel)
NEXT_PUBLIC_VERCEL_ANALYTICS=false

# -----------------------------------------------------------------------------
# Development Tools (Development only)
# -----------------------------------------------------------------------------

# Enable React strict mode
NEXT_PUBLIC_STRICT_MODE=true

# Enable debug mode (shows console logs)
NEXT_PUBLIC_DEBUG=false

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1

# -----------------------------------------------------------------------------
# GitHub API Rate Limiting (Optional)
# -----------------------------------------------------------------------------

# Show rate limit warnings in console
NEXT_PUBLIC_SHOW_RATE_LIMIT_WARNINGS=true

# Rate limit buffer (requests to leave unused)
NEXT_PUBLIC_RATE_LIMIT_BUFFER=100

# =============================================================================
# IMPORTANT NOTES:
# =============================================================================
# 1. This app works WITHOUT a database - uses browser localStorage!
# 2. Most variables are optional - the app works out of the box
# 3. You can login directly via the UI with your GitHub token
# 4. Only set OAuth variables if you want OAuth instead of token login
# 5. Copy this to .env.local if you want to customize settings
# =============================================================================

# =============================================================================
# QUICK START (NO SETUP REQUIRED):
# =============================================================================
# [ ] Just run: npm run dev
# [ ] Get GitHub token: https://github.com/settings/tokens/new?scopes=repo,user,read:org
# [ ] Paste token in the login page
# [ ] That's it! No .env needed for basic usage 🚀
# =============================================================================

# =============================================================================
# OPTIONAL: OAuth Setup (Only if you prefer OAuth over token login)
# =============================================================================
# [ ] Go to: https://github.com/settings/developers
# [ ] Create new OAuth App
# [ ] Set Homepage URL: http://localhost:3000
# [ ] Set Callback URL: http://localhost:3000/auth/callback
# [ ] Copy Client ID and Secret to .env.local
# [ ] Update the login flow to use OAuth (requires code changes)
# =============================================================================
